# ── Disabled ──────────────────────────────────────────────────────
# This monolithic CI workflow has been replaced with per-service CI/CD files:
#
#   frontend-cicd.yml        — push to main (Frontend/**)
#   api-gateway-cicd.yml     — push to main (api-gateway/**)
#   image-service-deploy.yml — workflow_dispatch by Lambda (S3 model upload)
#   text-service-deploy.yml  — workflow_dispatch by Lambda (S3 model upload)
#
# Lambda ONLY triggers image-service-deploy.yml and text-service-deploy.yml.
# It never triggers frontend or api-gateway workflows.
# ─────────────────────────────────────────────────────────────────
on: {}
